<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.colorsmash">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity android:name=".GameActivity"></activity>
        <activity android:name=".GameFunctionalityActivity" />
        <activity android:name=".ShowPreviousScoresActivity" />
        <activity android:name=".UserGameActivity" />
        <activity android:name=".UserOptionsActivity" />
        <activity android:name=".ChangeUserColorsActivity" />
        <activity android:name=".ChangeUserColosActivity" />
        <activity android:name=".LeaderBoardActivity" />
        <activity android:name=".StartGameActivity" />
        <activity android:name=".PersonalInfo" />
        <activity android:name=".ShowUsers" />
        <activity android:name=".AdminOptions" />
        <activity
            android:name=".AdminLoginActivity"
            android:label="@string/title_activity_admin_login" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>