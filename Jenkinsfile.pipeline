pipeline {
    agent { docker { image 'cangol/android-gradle' } }
    environment {HOME = '/tmp'} 
    stages {
    stage('Git') {
      // Get some code from a GitHub repository
      steps{
          //git 'https://github.com/markrity/android-unit-test-example.git'
      }
   }

    stage('Run Tests'){
          steps{
               sh "./gradlew"
          }
    }
}
}
